#!/usr/bin/env bash
set -euo pipefail

APP_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$APP_DIR"

if [[ ! -x build/packetlens ]]; then
    cmake --build build
fi

exec "$APP_DIR/build/packetlens" "$@"
